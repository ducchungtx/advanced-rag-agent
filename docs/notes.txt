Khung dự án Advanced RAG Agent (đã scaffold trong repo root).

Tài liệu chính:
- docs/architecture.md — kiến trúc hệ thống + sơ đồ Mermaid
- docs/learning-guide.md — hướng dẫn học tập / onboarding

Trạng thái: Phase 3 — ReAct + Redis semantic cache + RBAC + cross-encoder rerank (20→5) + Ragas Faithfulness (offline).
Cần: docker compose up -d redis; sau khi thêm audience metadata phải re-ingest; tăng CORPUS_VERSION khi đổi index.
Smoke rerank: python -u scripts/rerank_smoke.py
Eval: uv run --extra eval python -m eval.run_ragas
